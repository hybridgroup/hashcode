.row
  .span16
    "What programming languages have you used this year? Tweet using #code2011"
    %br/
    %br/

    %img{:src => Gchart.bar(:size => '600x200', :theme => :keynote, :data => @stats.collect{|s|s[1]})}

    %table.zebra-striped
      %th
        Language
      %th
        Count
      - @stats.each do |s|
        %tr
          %td
            #{s[0]}
          %td
            #{s[1]}
