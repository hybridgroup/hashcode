<div id="chart"></div>

<div class="container-raw">
  <h2>Raw Data</h2>

  <table class="raw-data">
    <thead>
      <tr>
      <th>Language</th>
      <th>Count</th>
      </tr>
    </thead>
    <tbody>
      <% @stats.each do |name, count| %>
        <tr>
          <td><a href="https://twitter.com/search?q=%23code2013%20<%= name %>&src=typd&f=realtime" target="blank"><%= name %></a></td>
          <td class="text-center"><%= count %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>